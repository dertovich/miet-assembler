#include "prog.h"

int main(int argc, char const *argv[])
{
	/* if (!data_size())
		std::cout << "Check file! (data_size.txt)" << std::endl;
	else
		std::cout << "File did not open!" << std::endl;*/
	check16(argc, argv);
	//check32(argc, argv);
	//check64(argc, argv);
	//printDump(void	*p, size_t	N);

	return 0;
}